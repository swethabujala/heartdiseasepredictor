{
  "name": "heart-attack-prediction",
  "version": "1.0.0",
  "private": true,
  "proxy": "http://localhost:5000",
  "scripts": {
    "start": "concurrently \"cd frontend && npm run dev\" \"cd backend && python app.py\"",
    "dev": "vite",
    "backend": "cd backend && python app.py",
    "frontend": "cd frontend && vite"
  },
  "dependencies": {
    "axios": "^1.4.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1"
  },
  "devDependencies": {
    "concurrently": "^8.0.2"
  }
}
